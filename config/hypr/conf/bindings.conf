# =============================================================
# Apps
# =============================================================
bind = $mainMod, Return, exec, $terminal
bind = $mainMod, Q,      exec, $terminal       # legacy
bind = $mainMod, B,      exec, $browser
bind = $mainMod, E,      exec, $fileManager
bind = $mainMod, Space,  exec, $menu
bind = $mainMod, R,      exec, $menu           # legacy
bind = $mainMod, V,      exec, cliphist list | wofi --dmenu | cliphist decode | wl-copy
bind = $mainMod SHIFT, D, exec, nwg-displays

# =============================================================
# Window management
# =============================================================
bind = $mainMod,       W,    killactive,
bind = $mainMod,       C,    killactive,        # legacy
bind = $mainMod SHIFT, Space, togglefloating,
bind = $mainMod,       F,    fullscreen, 0
bind = $mainMod SHIFT, F,    fullscreen, 1
bind = $mainMod,       P,    pseudo,
bind = $mainMod,       J,    layoutmsg, togglesplit
bind = $mainMod,       Tab,  cyclenext, prev

# =============================================================
# System
# =============================================================
bind = $mainMod,       L,      exec, loginctl lock-session
bind = $mainMod SHIFT, L,      exec, ~/.config/hypr/scripts/screensaver-start.sh
bind = $mainMod,       Escape, exec, wlogout -p layer-shell
bind = CTRL ALT,       Delete, exec, wlogout -p layer-shell
bind = $mainMod SHIFT, M,      exec, hyprctl dispatch exit

# =============================================================
# Focus (arrows + vim keys)
# =============================================================
bind = $mainMod, left,  movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up,    movefocus, u
bind = $mainMod, down,  movefocus, d
bind = $mainMod, h,     movefocus, l
bind = $mainMod, k,     movefocus, u
bind = $mainMod, j,     movefocus, d
# Note: vim 'l' (focus right) intentionally omitted — Super+L is reserved for lock

# =============================================================
# Move windows
# =============================================================
bind = $mainMod SHIFT, left,  movewindow, l
bind = $mainMod SHIFT, right, movewindow, r
bind = $mainMod SHIFT, up,    movewindow, u
bind = $mainMod SHIFT, down,  movewindow, d
bind = $mainMod SHIFT, h,     movewindow, l
bind = $mainMod SHIFT, k,     movewindow, u
bind = $mainMod SHIFT, j,     movewindow, d

# =============================================================
# Resize windows
# =============================================================
binde = $mainMod CTRL, left,  resizeactive, -40 0
binde = $mainMod CTRL, right, resizeactive,  40 0
binde = $mainMod CTRL, up,    resizeactive,  0 -40
binde = $mainMod CTRL, down,  resizeactive,  0  40

# =============================================================
# Workspaces
# =============================================================
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

bind = $mainMod, bracketleft,  workspace, e-1
bind = $mainMod, bracketright, workspace, e+1
bind = $mainMod, mouse_down,   workspace, e+1
bind = $mainMod, mouse_up,     workspace, e-1

# Scratchpad
bind = $mainMod,       S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# =============================================================
# Mouse
# =============================================================
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# =============================================================
# Wallpaper
# =============================================================
bind = $mainMod SHIFT, W, exec, ~/.config/hypr/scripts/wallpaper.py rotate
bind = $mainMod CTRL,  W, exec, ~/.config/hypr/scripts/wallpaper.py fetch

# =============================================================
# Screenshots (hyprshot saves to ~/Pictures + clipboard)
# =============================================================
bind = ,             Print, exec, hyprshot -m region
bind = $mainMod,     Print, exec, hyprshot -m window
bind = SHIFT,        Print, exec, hyprshot -m output

# =============================================================
# Media keys (with OSD)
# =============================================================
bindel = , XF86AudioRaiseVolume,  exec, swayosd-client --output-volume raise
bindel = , XF86AudioLowerVolume,  exec, swayosd-client --output-volume lower
bindel = , XF86AudioMute,         exec, swayosd-client --output-volume mute-toggle
bindel = , XF86AudioMicMute,      exec, swayosd-client --input-volume  mute-toggle
bindel = , XF86MonBrightnessUp,   exec, swayosd-client --brightness raise
bindel = , XF86MonBrightnessDown, exec, swayosd-client --brightness lower

bindl = , XF86AudioNext,  exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay,  exec, playerctl play-pause
bindl = , XF86AudioPrev,  exec, playerctl previous

# Caps Lock OSD
bindr = , Caps_Lock, exec, swayosd-client --caps-lock
